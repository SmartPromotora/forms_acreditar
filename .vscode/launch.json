{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "pwa-chrome",
      "request": "launch",
      "name": "Abrir no Chrome (index.html)",
      "file": "${workspaceFolder}/index.html"
    },
    {
      "type": "pwa-chrome",
      "request": "launch",
      "name": "Abrir no Chrome (localhost:8080)",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
